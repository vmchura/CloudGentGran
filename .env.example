# Catalunya Open Data Pipeline - Environment Configuration Template
# Copy this file to .env and configure your specific values

# =============================================================================
# AWS Configuration
# =============================================================================
# AWS CLI profile to use for deployments
AWS_PROFILE=default

# AWS region for resource deployment
AWS_REGION=eu-south-2

# AWS Account ID (optional - will be auto-detected if not specified)
# AWS_ACCOUNT_ID=123456789012

# =============================================================================
# Project Configuration
# =============================================================================
# Project name (used for resource naming)
PROJECT_NAME=catalunya-gent-gran

# Environment (development, staging, production)
ENVIRONMENT=development

# =============================================================================
# Data Pipeline Configuration
# =============================================================================
# S3 bucket name prefix (actual bucket will be {PROJECT_NAME}-{ENVIRONMENT}-{random})
S3_BUCKET_PREFIX=catalunya-gent-gran

# Data retention periods (in days)
LANDING_RETENTION_DAYS=1
STAGING_RETENTION_DAYS=90
MARTS_RETENTION_DAYS=90

# =============================================================================
# Lambda Configuration
# =============================================================================
# Lambda function timeout (in seconds, max 900 for 15 minutes)
LAMBDA_TIMEOUT=300

# Lambda memory allocation (in MB, between 128-10240)
LAMBDA_MEMORY=512

# =============================================================================
# API Configuration
# =============================================================================
# Catalunya Open Data API base URL
CATALUNYA_API_BASE_URL=https://analisi.transparenciacatalunya.cat

# API rate limiting (requests per minute)
API_RATE_LIMIT=60

# API timeout (in seconds)
API_TIMEOUT=30